<div class="flex flex-col gap-5 p-5">
   <div id="my-projects-page" class="flex flex-col gap-8">
      <div class="bg-gray-900/80 rounded-2xl shadow-2xl p-8 backdrop-blur-md border border-indigo-500/10">
         <h1 class="heading-1 mb-5"><%= t('.title') %></h1>
         <p class="subtitle mb-5"><%= t('.subtitle') %></p>
      </div>
   </div>

   <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
      <%= render partial: 'home/partials/project', collection: @projects, as: :project %>
   </div>
</div>